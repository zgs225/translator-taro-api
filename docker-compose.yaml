version: '3'
services: 
    api:
        build: .
        ports:
            - '8080:8080'
        volumes:
            - .:/code
        command:
            - translator-api
            - serve
            - --config=/code/config.production.yaml
    api2:
        build: .
        ports:
            - '8081:8080'
        volumes:
            - .:/code
        command:
            - translator-api
            - serve
            - --config=/code/config.production.yaml
    redis:
        image: 'redis:6'
